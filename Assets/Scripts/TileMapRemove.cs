using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TileMapRemove : MonoBehaviour
{
    PlayerController moveControls;
    // Start is called before the first frame update
    void Start()
    {
        moveControls = GetComponent<PlayerController>();
    }

    // Update is called once per frame
    void Update()
    {

    }
}
